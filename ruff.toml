# Ruff configuration for NIDS Platform
# Excludes legacy V1 reference files and environment folders from lint scope.

force-exclude = true

exclude = [
    ".venv",
    "venv",
    "v1_reference",
    "v1_reference/*",
    "v1_reference/**",
    "preprocessing.py",
    "preprocessing_ooc.py",
]
